import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		padding: 10,
	},
	containerMessage: {
		padding: 5,
		backgroundColor: "#eee",
		borderRadius: 7,
	},
});

export default styles;
